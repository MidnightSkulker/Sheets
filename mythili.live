#!/usr/local/bin/bash
# Need bash 4 or greater.
# Argument 1 is month abbreviation, e.g. Jan, Feb.
if [[ $# -ne 1 ]]
then
	echo "Must have 1 argument: Month abbreviation"
else
	echo "Processing for month ${1}"
  python3 main.py --SheetID=1MolY8_4Ze-g8vxVhd9MLg0yCq8asqY-JAk4q673zlf8 --SheetName=MythilisClass --Mode=Live --Month=$1
fi
